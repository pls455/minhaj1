import { auth, db } from "./firebase.js";
import { collection, getDocs, query, limit, addDoc, updateDoc, doc, serverTimestamp } from "https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js";
import { analyzeGroup, analyzeSource, learnApproved } from "./source-intelligence.js";
const $=id=>document.getElementById(id);const esc=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
let branches=[],subjects=[],categories=[],discovered=[];
const DRIVE_FOLDER="application/vnd.google-apps.folder";
const norm=s=>String(s||"").toLowerCase().normalize("NFKC").replace(/[أإآ]/g,"ا").replace(/ة/g,"ه").replace(/[\u064B-\u065F]/g,"").replace(/[^\u0600-\u06FFa-z0-9]+/g," ").replace(/\s+/g," ").trim();
const apiKey=()=>localStorage.getItem("minhaj_drive_api_key")||$("driveApiKey")?.value.trim()||"";
const directFileUrl=id=>`https://drive.google.com/file/d/${encodeURIComponent(id)}/view`;
const iconForMime=(m="",n="")=>{const x=`${m} ${n}`.toLowerCase();if(x.includes("pdf"))return"📄";if(x.includes("video")||/\.(mp4|mov|webm)$/i.test(n))return"🎥";if(x.includes("image")||/\.(png|jpg|jpeg|webp)$/i.test(n))return"🖼️";return"📎"};
const setMessage=(t,error=false)=>{const e=$("driveMsg");if(e){e.textContent=t||"";e.className=t?`message ${error?"error":"success"}:"message"}};
async function loadReferenceData(){const get=async n=>(await getDocs(query(collection(db,n),limit(2000)))).docs.map(d=>({id:d.id,...d.data()}));[branches,subjects,categories]=await Promise.all([get("branches"),get("subjects"),get("categories")])}
const options=(items,placeholder,selected="")=>`<option value="">${esc(placeholder)}</option>`+items.filter(x=>x.active!==false).sort((a,b)=>(a.order??999)-(b.order??999)).map(x=>`<option value="${esc(x.id)}" ${String(x.id)===String(selected)?"selected":""}>${esc(x.name)}</option>`).join("");
const branchChecks=(selected=[])=>{const s=new Set((selected||[]).map(String));return branches.filter(x=>x.active!==false).map(x=>`<label class="drive-branch-check"><input type="checkbox" data-branch-id="${esc(x.id)}" ${s.has(String(x.id))?"checked":""}>${esc(x.name)}</label>`).join("")};
const branchNames=ids=>(ids||[]).map(id=>branches.find(x=>String(x.id)===String(id))?.name||id).join("، ")||"غير محدد";
function driveIdFromUrl(v){try{const u=new URL(String(v||"").trim());if(!/drive\.google\.com$/i.test(u.hostname))return null;let m=u.pathname.match(/\/file\/d\/([\w-]+)/);if(m)return{id:m[1],kind:"file"};m=u.pathname.match(/\/folders\/([\w-]+)/);if(m)return{id:m[1],kind:"folder"};const id=u.searchParams.get("id");if(id)return{id,kind:u.pathname.includes("folder")?"folder":"file"}}catch{}return null}
async function driveApi(path,params={}){const key=apiKey();if(!key)throw new Error("أضف Google Drive API Key لفحص المجلدات.");const u=new URL(`https://www.googleapis.com/drive/v3/${path}`);Object.entries({...params,key}).forEach(([k,v])=>u.searchParams.set(k,v));const r=await fetch(u);const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.error?.message||`Drive API ${r.status}`);return d}
async function inspectDrive(value){const p=driveIdFromUrl(value);if(!p)throw new Error("هذا ليس رابط Google Drive صالحًا.");if(!apiKey()&&p.kind==="file")return{root:{id:p.id,name:"ملف Google Drive",mimeType:"application/octet-stream",webViewLink:directFileUrl(p.id),kind:"file"},items:[{id:p.id,name:"ملف Google Drive",mimeType:"application/octet-stream",webViewLink:directFileUrl(p.id),kind:"file",ancestors:[]}]};const root=await driveApi(`files/${encodeURIComponent(p.id)}`,{fields:"id,name,mimeType,size,modifiedTime,webViewLink,parents,trashed,description",supportsAllDrives:"true"});if(root.trashed)throw new Error("هذا العنصر موجود في سلة المهملات.");if(root.mimeType!==DRIVE_FOLDER)return{root:{...root,kind:"file"},items:[{...root,kind:"file",ancestors:[]}]};const items=[];async function walk(parent,depth,path){if(depth>15||items.length>=3000)return;let token="";do{const d=await driveApi("files",{q:`'${parent}' in parents and trashed = false`,fields:"nextPageToken,files(id,name,mimeType,size,modifiedTime,webViewLink,parents,trashed,description)",pageSize:"1000",orderBy:"folder,name",supportsAllDrives:"true",includeItemsFromAllDrives:"true",...(token?{pageToken:token}:{})});for(const x of d.files||[]){const item={...x,kind:x.mimeType===DRIVE_FOLDER?"folder":"file",depth,ancestors:path};if(item.kind==="folder")await walk(item.id,depth+1,[...path,item.name]);else items.push(item);if(items.length>=3000)break}token=d.nextPageToken||""}while(token&&items.length<3000)}await walk(root.id,0,[root.name]);return{root:{...root,kind:"folder"},items}}
function relationValid(subjectId,categoryId,branchIds){const s=subjects.find(x=>String(x.id)===String(subjectId));const c=categories.find(x=>String(x.id)===String(categoryId));const bs=(branchIds||[]).map(String);if(!s||!c)return{ok:true,reason:"بيانات غير مكتملة"};const cSubjects=[c.subjectId,c.subjectID,...(c.subjectIds||[])].filter(Boolean).map(String);const cBranches=[c.branchId,c.branchID,...(c.branchIds||[])].filter(Boolean).map(String);const sBranches=[s.branchId,s.branchID,...(s.branchIds||s.branches||[])].filter(Boolean).map(String);if(cSubjects.length&&!cSubjects.includes(String(s.id)))return{ok:false,reason:"التصنيف لا يتبع المادة"};if(cBranches.length&&bs.length&&!bs.some(x=>cBranches.includes(x)))return{ok:false,reason:"التصنيف لا يتبع الفرع"};if(sBranches.length&&bs.length&&!bs.some(x=>sBranches.includes(x)))return{ok:false,reason:"المادة لا تتبع الفرع"};return{ok:true,reason:""}}
async function classify(files){try{return await analyzeGroup(files)}catch(e){return Promise.all(files.map(async x=>({item:x,result:await analyzeSource(x)})))}}
function renderDiscovered(result){discovered=result.items||[];const box=$("driveResults");if(!box)return;const files=discovered.filter(x=>x.kind!=="folder"),folders=discovered.filter(x=>x.kind==="folder");if(!files.length){box.innerHTML=`<div class="empty">تم الفحص، ولا توجد ملفات.</div>`;return}setMessage("جاري تحليل الملفات والمجموعات...");classify(files).then(rows=>{const byId=new Map(rows.map(x=>[x.item.id,x.result]));const analyzed=files.map(x=>byId.get(x.id)||{subjectId:"",categoryId:"",branchIds:[],confidence:{},needsReview:true,conflicts:["تعذر التحليل"]});const clear=analyzed.filter(c=>isAutoSafe(c)).length;box.innerHTML=`<div class="drive-result-head"><strong>📁 ${esc(result.root.name)}</strong><span>${files.length} ملف • ${folders.length} مجلد • ${clear} جاهز تلقائيًا • ${files.length-clear} يحتاج تحقق</span></div><div class="drive-review-note">🧠 فحص فقط. المحرك يستخدم كامل المسار + المجموعة + المعرفة السابقة، ولا يفرض تصنيفًا عندما لا توجد أدلة.</div><div class="drive-items">${files.map((x,i)=>cardHtml(x,analyzed[i],i)).join("")}</div><div class="drive-import-row"><button id="importDriveItems" class="btn primary">📥 إضافة المحدد للمراجعة</button></div>`;$("importDriveItems")?.addEventListener("click",()=>importForReview(analyzed));setMessage("اكتمل الفحص الذكي. لم تتم إضافة أي مصدر.")}).catch(e=>setMessage(`تعذر التصنيف الذكي: ${e.message}`,true))}
function isAutoSafe(c){const avg=Object.values(c.confidence||{}).filter(Number.isFinite);const score=avg.length?avg.reduce((a,b)=>a+b,0)/avg.length:0;return !c.needsReview&&!c.conflicts?.length&&score>=85&&relationValid(c.subjectId,c.categoryId,c.branchIds).ok}
function cardHtml(x,c,i){const s=subjects.find(a=>String(a.id)===String(c.subjectId)),cat=categories.find(a=>String(a.id)===String(c.categoryId));const avg=Math.round(Object.values(c.confidence||{}).filter(Number.isFinite).reduce((a,b)=>a+b,0)/(Object.values(c.confidence||{}).filter(Number.isFinite).length||1));const rel=relationValid(c.subjectId,c.categoryId,c.branchIds);const safe=isAutoSafe(c);return`<div class="drive-review-card" data-review-index="${i}"><div class="drive-file-head"><input type="checkbox" data-drive-index="${i}" checked><span>${iconForMime(x.mimeType,x.name)}</span><strong title="${esc(x.name)}">${esc(x.name)}</strong></div><div class="drive-auto-hint">🤖 المادة: ${esc(s?.name||"غير محددة")} • التصنيف: ${esc(cat?.name||"غير محدد")} • الفرع: ${esc(branchNames(c.branchIds))} • الثقة: ${avg}% ${safe?"• ✅ متوافق":"• ⚠️ يحتاج مراجعة"}</div>${c.conflicts?.length?`<div class="message error">⚠️ ${esc(c.conflicts.join("، "))}</div>`:""}${!rel.ok?`<div class="message error">🚫 ${esc(rel.reason)}</div>`:""}<div class="drive-path">📂 ${esc((x.ancestors||[]).join(" / ")||"الجذر")}</div><div class="drive-classify"><select data-subject>${options(subjects,"المادة",c.subjectId)}</select><select data-category>${options(categories,"التصنيف",c.categoryId)}</select><div class="drive-branch-wrap"><span>الفروع:</span>${branchChecks(c.branchIds)}</div></div></div>`}
async function importForReview(results){try{const cards=[...document.querySelectorAll(".drive-review-card[data-review-index]")];const selected=cards.filter(c=>c.querySelector("[data-drive-index]")?.checked).map(card=>({card,item:discovered[Number(card.dataset.reviewIndex)]}));if(!selected.length)return setMessage("حدد مصدرًا واحدًا على الأقل.",true);const snap=await getDocs(query(collection(db,"sourceRegistry"),limit(5000)));const existing=new Set(snap.docs.map(d=>d.data()?.driveId).filter(Boolean));let added=0,skipped=0,blocked=0;for(const z of selected){const card=z.card,item=z.item;const subjectId=card.querySelector("[data-subject]")?.value||"",categoryId=card.querySelector("[data-category]")?.value||"",branchIds=[...card.querySelectorAll("[data-branch-id]:checked")].map(x=>x.dataset.branchId);if(existing.has(item.id)){skipped++;continue}const rel=relationValid(subjectId,categoryId,branchIds);if(!rel.ok){blocked++;continue}await addDoc(collection(db,"sourceRegistry"),{provider:"Google Drive",sourceType:"google_drive_file",driveId:item.id,title:item.name||"مصدر Google Drive",url:item.webViewLink||directFileUrl(item.id),mimeType:item.mimeType||"",size:item.size?Number(item.size):null,modifiedTime:item.modifiedTime||null,detectedPath:(item.ancestors||[]).join(" / "),branchIds,subjectId,categoryId,status:"pending_review",active:false,autoSuggested:isAutoSafe(results[Number(card.dataset.reviewIndex)]),createdBy:auth.currentUser?.uid||"",createdByEmail:auth.currentUser?.email||"",createdAt:serverTimestamp(),updatedAt:serverTimestamp()});existing.add(item.id);added++}await loadReviewQueue();setMessage(`تمت إضافة ${added} للمراجعة • مكرر: ${skipped} • مُنع بسبب تعارض: ${blocked}`)}catch(e){console.error(e);setMessage(`تعذر إضافة المصادر: ${e.message||e}`,true)}}
async function loadReviewQueue(){const box=$("driveReviewList");if(!box)return;try{const snap=await getDocs(query(collection(db,"sourceRegistry"),limit(5000)));const pending=snap.docs.map(d=>({id:d.id,...d.data()})).filter(x=>x.status==="pending_review");box.innerHTML=pending.length?pending.map(x=>`<div class="drive-review-card" data-pending-id="${esc(x.id)}"><div class="drive-file-head"><span>${iconForMime(x.mimeType,x.title)}</span><strong>${esc(x.title)}</strong></div><div class="drive-review-meta">📂 ${esc(x.detectedPath||"غير متوفر")}</div><div class="drive-classify"><select data-review-subject>${options(subjects,"المادة",x.subjectId||"")}</select><select data-review-category>${options(categories,"التصنيف",x.categoryId||"")}</select><div class="drive-branch-wrap">${branchChecks(x.branchIds||[])}</div></div><div class="drive-review-actions"><button class="btn primary" data-approve>✓ اعتماد ونشر</button><button class="btn secondary" data-reject>رفض</button></div></div>`).join(""):"<div class=\"empty\">لا توجد مصادر بانتظار المراجعة.</div>";box.querySelectorAll("[data-approve]").forEach(b=>b.onclick=()=>approveReview(b.closest("[data-pending-id]")));box.querySelectorAll("[data-reject]").forEach(b=>b.onclick=()=>rejectReview(b.closest("[data-pending-id]")))}catch(e){box.innerHTML=`<div class="message error">تعذر تحميل المراجعة: ${esc(e.message||e)}</div>`}}
async function approveReview(card){try{const id=card?.dataset.pendingId;if(!id)return;const subjectId=card.querySelector("[data-review-subject]")?.value||"",categoryId=card.querySelector("[data-review-category]")?.value||"",branchIds=[...card.querySelectorAll("[data-branch-id]:checked")].map(x=>x.dataset.branchId);if(!subjectId||!categoryId)return alert("حدد المادة والتصنيف. الفرع اختياري إذا لم يكن معروفًا.");const rel=relationValid(subjectId,categoryId,branchIds);if(!rel.ok)return setMessage(`🚫 لا يمكن النشر: ${rel.reason}`,true);const snap=await getDocs(query(collection(db,"sourceRegistry"),limit(5000)));const found=snap.docs.find(d=>d.id===id);if(!found)return;const source=found.data();const duplicate=snap.docs.some(d=>d.id!==id&&d.data()?.driveId===source.driveId&&d.data()?.status==="published");if(duplicate)return setMessage("🚫 هذا الرابط منشور مسبقًا.",true);await addDoc(collection(db,"resources"),{title:source.title,url:source.url,subjectId,branchIds,categoryId,type:source.mimeType||"Google Drive",description:"مصدر مستضاف على Google Drive",keywords:[],active:true,order:9999,stableId:`drive-${source.driveId}`,driveId:source.driveId,sourceRegistryId:id,createdAt:serverTimestamp(),updatedAt:serverTimestamp()});await updateDoc(doc(db,"sourceRegistry",id),{subjectId,categoryId,branchIds,status:"published",active:true,publishedBy:auth.currentUser?.uid||"",publishedAt:serverTimestamp(),updatedAt:serverTimestamp()});await learnApproved(source,{subjectId,categoryId,branchIds,groupKey:norm(source.detectedPath||""),confidence:{adminApproved:true}});await loadReviewQueue();setMessage("تم اعتماد المصدر ونشره، وتم تعليم النظام من القرار.")}catch(e){console.error(e);setMessage(`تعذر اعتماد المصدر: ${e.message||e}`,true)}}
async function rejectReview(card){try{const id=card?.dataset.pendingId;if(!id)return;await updateDoc(doc(db,"sourceRegistry",id),{status:"rejected",active:false,rejectedBy:auth.currentUser?.uid||"",rejectedAt:serverTimestamp(),updatedAt:serverTimestamp()});await loadReviewQueue();setMessage("تم رفض المصدر.")}catch(e){setMessage(`تعذر رفض المصدر: ${e.message||e}`,true)}}
async function init(){if(!$("driveSourcesPanel"))return;try{await loadReferenceData()}catch(e){setMessage(`تعذر تحميل بيانات التصنيف: ${e.message}`,true)}if($("driveApiKey")){const saved=localStorage.getItem("minhaj_drive_api_key");if(saved)$("driveApiKey").value=saved}$("saveDriveApiKey")?.addEventListener("click",()=>{const k=$("driveApiKey")?.value.trim()||"";k?localStorage.setItem("minhaj_drive_api_key",k):localStorage.removeItem("minhaj_drive_api_key");setMessage(k?"تم حفظ المفتاح على هذا الجهاز.":"تم حذف المفتاح.")});$("inspectDriveBtn")?.addEventListener("click",async()=>{setMessage("جاري فحص Google Drive وتحليل المجموعات...");try{const r=await inspectDrive($("driveUrl")?.value||"");renderDiscovered(r)}catch(e){setMessage(e.message||"تعذر فحص الرابط.",true)}});$("refreshDriveReview")?.addEventListener("click",loadReviewQueue);window.addEventListener("minhaj:drive-panel-open",loadReviewQueue);await loadReviewQueue()}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init);else init();